#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    ;
    cin >> t;
    while (t--)
    {
        long long int num;
        cin >> num;

        cout << fixed << abs((long long int)num * 4) << endl;
    }
    return 0;
}