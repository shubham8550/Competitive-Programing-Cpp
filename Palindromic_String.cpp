#include <bits/stdc++.h>

using namespace std;

int main()
{
    string rev, s;
    cin >> s;
    for (int i = s.size() - 1; i >= 0; rev.push_back(s[i--]))
    {
    }
    cout << (rev == s ? "YES" : "NO") << endl;
    return 0;
}